"""The programme's registrations and the check suites that gate them.

The markdown alongside this package states what was claimed and when: the warrant
ledger, the falsification battery, the pre-registrations and their results. The modules
under ``research.checks`` are the falsifiers those claims are measured by.

Not distributed. It is a workspace member so that the checks get the same lint, type and
import resolution the library gets, rather than running off ``sys.path``.
"""
