"""Runnable explorations, kept beside the write-ups that quote them.

Nothing here emits a `CheckReport` or appears in `research/registered_checks.toml`. An
exploration has no warrant to report: it exists so a number printed in a write-up can be
regenerated and argued with rather than taken on the text's word.
"""
